name: Publish

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  publish-npm:
    name: Publish to npm
    runs-on: ubuntu-latest
    permissions:
      contents: write   # create the GitHub release
      id-token: write   # npm provenance
    steps:
      - uses: actions/checkout@v5

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3"

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          registry-url: "https://registry.npmjs.org"

      # npm OIDC trusted publishing (tokenless) requires npm >= 11.5.1.
      - name: Upgrade npm for OIDC trusted publishing
        run: npm install -g npm@latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Typecheck
        run: bun run check

      - name: Build package
        run: bun run build

      # Auth is via OIDC trusted publisher (configured on npmjs.com) — no token.
      - name: Publish to npm
        run: |
          version=$(node -p "require('./package.json').version")
          if npm view "@hiai-gg/hiai-ui@${version}" version >/dev/null 2>&1; then
            echo "::notice::@hiai-gg/hiai-ui@${version} already on npm — skipping."
          else
            npm publish --access public --provenance --ignore-scripts
          fi

      - name: Create GitHub release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          draft: false
          prerelease: ${{ contains(github.ref_name, '-') }}
